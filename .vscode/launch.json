{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/build/scoundrills",
            "args": [],
            "cwd": "${workspaceFolder}/build",
            "preLaunchTask": "build",
        }
    ]
}